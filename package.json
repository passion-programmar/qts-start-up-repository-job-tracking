{
  "name": "qts-startup",
  "version": "1.2.0",
  "private": true,
  "description": "QTS_Startup — job capture system (API + web UI)",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "prestart": "node scripts/setup.mjs",
    "start": "concurrently -k -n api,ui -c blue,green \"npm run start --prefix server\" \"npm run dev --prefix admin-web\"",
    "build": "npm run build --prefix server && npm run build --prefix admin-web",
    "stop": "node scripts/stop.mjs",
    "db:reset": "npm run db:reset --prefix server",
    "db:clear-bidders": "npm run db:clear-bidders --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vercel": "^54.17.1"
  }
}
